Phase 013 verification and evidence

- retention-automation slice landed first:
  - added `knocker.RetentionPolicy`
  - added `Knocker.run_retention(policy, *, stop_event=None, on_error=None)`
  - automated retention now runs through an internal Honker maintenance queue
    and the shared core retention-pass primitive
  - no-op automated prunes still write durable prune-audit rows
  - multiple runners on one SQLite file are safe; one process/instance should
    still own retention configuration
- added e2e retention-automation coverage in `tests/test_retention_automation.py`
- updated the public-surface proof in `tests/test_operator_reads.py`
- updated docs / roadmap / baseline truth for Python-first retention automation
- curated provider pack landed:
  - added built-in providers for `shopify`, `slack`, `postmark`, `resend`,
    `paddle`, and `lemon-squeezy`
  - added repo-owned provider metadata plus binding-neutral fixtures under
    `providers/<name>/`
  - expanded conformance coverage in `tests/test_provider_conformance.py`
  - added generic real-`receive(...)` ingress proofs for every curated
    provider in `tests/test_providers.py`
- minimal shared-contract bindings landed for:
  - Node (existing binding kept and proved)
  - Bun
  - Ruby
  - Go
  - Elixir
- each binding now proves the same baseline:
  - open/bootstrap
  - endpoint registration
  - ingest
  - one worker-processing path
  - `get_event`
  - `list_events`
  - `replay`
- repo truth was updated:
  - `Makefile` now has binding-specific targets for Bun, Ruby, Go, and Elixir
  - README / SYSTEM / ROADMAP now mention the expanded curated provider pack
    and the minimal extra bindings
  - added `/reference/sqlite-bindings/` to the docs site
- perf note:
  - the stable local benchmark harness now tears down cleanly using explicit
    `Knocker.close()` calls in the benchmark helpers
  - refreshed local baseline on this machine after the provider/binding work:
    - durable ingress-only: `5,000` events in `3.410s` (`1,466/s`, `0.682 ms/event`)
    - no-op-handler worker drain: `5,000` events in `1.523s` (`3,282/s`, `0.305 ms/event`)
  - no new throughput-oriented runtime code shipped in this slice; the worker
    throughput behavior still reflects the earlier phase-011/012 queue-path work
- verification:
  - `uv run --group dev pytest tests/test_retention_automation.py tests/test_operator_reads.py`
  - `uv run --group dev pytest tests/test_provider_conformance.py tests/test_providers.py`
  - `ruby packages/knocker-ruby/test/basic_test.rb`
  - `cd packages/knocker-go && go test ./...`
  - `cd packages/knocker-bun && bun test`
  - `cd packages/knocker-elixir && mix deps.get && mix test`
  - `make test`
  - `npm --prefix site run build`
