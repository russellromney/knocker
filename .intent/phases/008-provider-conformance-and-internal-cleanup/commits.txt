# 008-provider-conformance-and-internal-cleanup

Verification:

- `make test`
  - Rust: 12 passed
  - Python: 112 passed
  - Node: 2 passed
- `npm --prefix site run build`
  - 12 pages built (added `/guides/contributing-providers/`)
- Public docstring smoke
  - all public surface has docstrings (Knocker, Provider, ProviderRequest, ProviderResult plus their public methods and properties)
- File-length check
  - Python implementation files under 1000 lines (max: _knocker.py at 998)
  - Python test files under 1000 lines (max: test_providers.py at 872)

Module / file deltas:

- packages/knocker/python/knocker/_builtin_stripe.py — new
- packages/knocker/python/knocker/_builtin_github.py — new
- packages/knocker/python/knocker/job_payload.py — new (helpers moved from coercion.py)
- packages/knocker/python/knocker/coercion.py — trimmed to generic argument coercion
- packages/knocker/python/knocker/providers.py — public facade only; built-in classes moved out
- packages/knocker/python/knocker/verifiers.py — adds Provider-instance path through `_validate_instance_provider` and `_describe_provider`
- packages/knocker/python/knocker/_knocker.py — `app._queue` is internal; `app.queue_name` is the public read-only property
- providers/stripe/metadata.json + 4 fixtures (valid, invalid_signature, missing_signature_header, expired_timestamp)
- providers/github/metadata.json + 4 fixtures (valid, invalid_signature, missing_signature_header, missing_delivery_header)
- tests/test_provider_conformance.py — fixture-driven conformance suite with clock injection for Stripe
- tests/test_provider_surface.py — Provider-instance path + import-path stability (split out of test_providers.py for line-count standard)

Curated provider conformance test inventory (tests/test_provider_conformance.py):

- test_stripe_curated_fixture_conformance[valid]
- test_stripe_curated_fixture_conformance[invalid_signature]
- test_stripe_curated_fixture_conformance[missing_signature_header]
- test_stripe_curated_fixture_conformance[expired_timestamp]
- test_stripe_metadata_matches_python_implementation
- test_github_curated_fixture_conformance[valid]
- test_github_curated_fixture_conformance[invalid_signature]
- test_github_curated_fixture_conformance[missing_signature_header]
- test_github_curated_fixture_conformance[missing_delivery_header]
- test_github_metadata_matches_python_implementation
- test_catalog_metadata_files_share_required_fields
- test_fixture_files_share_required_schema_fields

Provider-instance path test inventory (tests/test_provider_surface.py):

- test_add_endpoint_accepts_provider_instance
- test_add_endpoint_provider_instance_rejects_builtin_name_collision
- test_add_endpoint_provider_instance_validates_name_and_version
- test_add_endpoint_provider_instance_rejects_non_provider_value
- test_add_endpoint_provider_instance_requires_secrets_for_required
- test_add_endpoint_provider_instance_validates_options
- test_add_endpoint_instance_provider_does_not_mutate_registry

Public surface stability test inventory (tests/test_provider_surface.py):

- test_public_provider_symbols_reachable_from_top_level
- test_app_queue_name_returns_configured_queue
- test_raw_queue_attribute_is_no_longer_public

Implementation pre-decisions (recorded in reviews_and_decisions.md):

- PD1 — Stripe `_StripeProvider(clock=...)` constructor injection
- PD2 — `add_endpoint(provider=ProviderInstance, ...)` instance path; curated string names reserved
- PD3 — `metadata.json` schema: name, version, support_tier, description, optional upstream_docs_url
- PD4 — fixture `request.secrets` is a list of UTF-8 strings
- PD5 — fixture `request.provider_options` parity with runtime; loader runs `validate_options(...)`
- PD6 — shared provider helpers stay in `providers.py`; built-in modules import from there
- PD7 — fixture body is a UTF-8 string encoded by the loader
- PD8 — Stripe fixtures declare `request.now_s` for absolute timestamp control

---

Implementation Review 1 follow-up (N1 + N2):

- `make test`
  - Rust: 12 passed
  - Python: 111 passed (was 112; net -1 after retiring 5 register_provider unit tests and adding 4 new tests covering the intended steady-state behavior plus 2 N1 regressions)
  - Node: 2 passed
- `npm --prefix site run build`
  - 12 pages built
- Public docstring smoke
  - all public surface has docstrings; `Knocker.register_provider` is removed (regression-locked by `test_public_provider_symbols_reachable_from_top_level`)
- File-length check
  - Python implementation files under 1000 lines (max: _knocker.py at 987)
  - Python test files under 1000 lines (max: test_providers.py at 787)

N1 fix (instance-path verifier reuse):

- Gated the previous-config verifier-reuse branch in `_resolve_endpoint_verifier(...)` on `instance is None`. The shortcut now only fires for the curated string-name re-registration path; the instance path always rebuilds.
- Regression tests in `tests/test_provider_surface.py`:
  - test_re_add_endpoint_with_new_instance_rebuilds_verifier_with_new_secrets
  - test_re_add_endpoint_with_new_instance_no_secrets_is_rejected

N2 fix (string-based custom-provider lookup retired):

- `Knocker.register_provider(...)` removed from the public class.
- `_validate_provider_for_registration` deleted from `verifiers.py` (its only caller was register_provider).
- Per-`Knocker` registry now fixed to curated built-ins; `add_endpoint(provider="X", ...)` resolves only `stripe` and `github`.
- `tests/test_providers.py`: 5 register_provider unit tests removed; 4 end-to-end app-local-provider tests migrated to the instance path. New positive coverage:
  - test_register_provider_method_is_no_longer_present
  - test_provider_versions_only_lists_curated_builtins
- Import-path stability test updated: now asserts `Knocker.register_provider` is *absent*.
- Docs (`verified-ingress.mdx`, `reference/python.mdx`) rewritten to drop the legacy string-name path; the instance path is the only documented path for non-curated providers.
- CHANGELOG: explicit `### Removed` (`register_provider`) and `### Fixed` (N1) entries added; `### Changed` entries updated.
- SYSTEM.md: current-baseline bullets updated.
